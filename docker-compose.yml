version: '3'

services:
  api:

    build: ./backend

    container_name: backend

    command: poetry run ./run.sh

    ports:
      - "8001:8001"

  frontend:

    build: ./frontend

    container_name: frontend

    command: npm run serve

    ports:
      - "8080:8080"