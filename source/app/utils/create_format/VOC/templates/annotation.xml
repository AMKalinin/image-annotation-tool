<annotation>
	<folder>{{ folder }}</folder>	
	<filename>{{ filename }}</filename>
	<path>{{ path }}</path>
	<source>
		<database>{{ database }}</database>
	</source>
	<size>
		<width>{{ width }}</width>
		<height>{{ height }}</height>
		<depth>{{ depth }}</depth>
	</size>
	<segmented>{{ segmented }}</segmented>
	{% for object in objects %}
		<object>
			<name>{{ object.name }}</name>
			<pose>{{ object.pose }}</pose>
			<truncated>{{ object.truncated }}</truncated>
			<difficult>{{ object.difficult }}</difficult>
			{% if 'points_p' in object.keys() %}
				<polygon>
					{% for point in object.points_p %}
						<x{{ loop.index }}>{{ point.0 }}</x{{ loop.index }}>
						<y{{ loop.index }}>{{ point.1 }}</y{{ loop.index }}>
					{% endfor %}
				</polygon>
			{% elif 'points_l' in object.keys() %}
				<line>
					{% for point in object.points_l %}
						<x{{ loop.index }}>{{ point.0 }}</x{{ loop.index }}>
						<y{{ loop.index }}>{{ point.1 }}</y{{ loop.index }}>
					{% endfor %}
				</line>
			{% elif 'points_c' in object.keys() %}
			<circle>
				<x>{{ object.points_c[0] }}</x>
				<y>{{ object.points_c[1] }}</y>
				<r>{{ object.points_c[2]}}</r>
			</circle>
			{% elif 'point_p' in object.keys() %}
				<point>
					{% for point in object.point_p %}
						<x{{ loop.index }}>{{ point.0 }}</x{{ loop.index }}>
						<y{{ loop.index }}>{{ point.1 }}</y{{ loop.index }}>
					{% endfor %}
				</point>
			{% endif %}
			<bndbox>
				<xmin>{{ object.xmin }}</xmin>
				<ymin>{{ object.ymin }}</ymin>
				<xmax>{{ object.xmax }}</xmax>
				<ymax>{{ object.ymax }}</ymax>
			</bndbox>
		</object>
	{% endfor %}
</annotation>